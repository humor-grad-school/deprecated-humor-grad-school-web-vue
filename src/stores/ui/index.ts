import menu from './menu';
import modal from './modal';
import snackbar from './snackbar';

export default {
    namespaced: true,
    modules: {
        menu,
        modal,
        snackbar
    }
};
